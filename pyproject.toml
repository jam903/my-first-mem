[project]
name = "your-first-memory"
version = "0.1.0"
description = "A memory for a model that has none — CAM session-5 starter (record quotes, list quotes, Neon Postgres)"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2,<2",       # pinned: mcp 2.x renamed FastMCP (also pinned in render.yaml — buildCommand bypasses this file)
    "psycopg[binary]>=3.1",
]
